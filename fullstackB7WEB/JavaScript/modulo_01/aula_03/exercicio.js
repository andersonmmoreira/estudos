// Substitua o @ pela condicional correta para que o resultado seja TRUE

let x = 10;
let y = 5
console.log(x > y);

// Substitua o @ pela condicional correta para que o resultado seja FALSE

let w = "10";
let z = 10;
console.log(w === z);

// Substitua o @ pela condicional correta para que o resultado seja TRUE

let a = 5;
let b = 10;
console.log(a != b);

// Crie uma condiciona para verificar se o preço da carne está mais barato ou caro. PS: Até 45 está mais barato.

let preco = 40.3;

if (preco <= 45) {
    console.log("Está mais BARATO!")
}